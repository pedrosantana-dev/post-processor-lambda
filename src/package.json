{
  "name": "pora-post-processor",
   "version": "1.0.0",
   "description": "AWS Lambda function to process scheduled posts from SQS",
   "main": "index.js",

   "scripts": {
     "test": "node test-syntax.js",
     "lint": "node --check index.js",
     "validate": "npm run lint && npm run test",
     "zip": "zip -r ../lambda-function.zip .",
     "deploy": "npm run zip && aws lambda update-function-code --function-name pora-scheduler-post-processor --zip-file fileb://lambda-function.zip"
   },
   "dependencies": {
    "@prisma/client": "^5.22.0",
    "@aws-sdk/client-sqs": "^3.658.1",
    "googleapis": "^144.0.0",
    "crypto-js": "^4.2.0",
    "mailgun.js": "10.2.3",
    "form-data": "4.0.0" 
   },
   "devDependencies": {
      "prisma": "^5.22.0"
   },
   "engines": {
     "node": ">=24.18.0"
   },
   "keywords": [
    "aws",
    "lambda",
    "sqs",
    "social-media",
    "scheduler",
    "youtube",
    "instagram",
    "facebook"

   ]




   




}
